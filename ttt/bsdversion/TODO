a) split into multiple files
b) add computer "hard mode" and "easy mode" (where computer can/can't win). atm it is easy only
c) use the symetrical winning approach (rotating the board and checking 3 spots)