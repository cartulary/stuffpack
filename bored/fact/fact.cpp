#include <iostream>
int main (int argc, char* argv[])
{
	unsigned long int num;

	// if the user gave us a value - do something with it
	if (argc > 1)
	{
		num = argv[1];
	}
	else
	{
	}
	for
	return 0;
}