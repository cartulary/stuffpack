1) continue work on tdb - the ports metadata thing
2) make a shell script to allow users to create a simple port Makefile.
3) Continue work on conflict finder
4) update symports /port/
5) make a porttools port...